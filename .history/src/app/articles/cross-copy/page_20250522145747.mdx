import { ArticleLayout } from '@/components/ArticleLayout'
import { VideoPlayer } from '@/components/VideoPlayer'
import Image from 'next/image'
import hero from './CC_homepage.png'
import mobile_example from './CC_mobile.png'
import dashboard_example from './Dashboard_Example.png'

import {
  InformationCircleIcon,
  CloudArrowUpIcon,
  LockClosedIcon,
  ServerIcon,
  ArrowTopRightOnSquareIcon,
} from '@heroicons/react/20/solid'

export const article = {
  author: 'Tom Carruthers',
  date: '2025-03-24',
  title: 'Cross Copy',
  description:
    'At its core, Cross Copy provides a simple way to avoid having to email text and files to yourself. It works in a similar way to Google Drive or Dropbox, but without the bloat of folders, subscriptions, or size limits.',
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

At it's core, Cross Copy just provides a simple way to avoid having
to email text and files to yourself. It works in a similar way to
Google Drive or Dropbox, but without the bloat of folders,
subscriptions, or size limits.

<Image src={hero} alt="The Cross Copy landing page." />

<ul className="mt-8 space-y-6 text-gray-300">
  <li className="flex gap-3">
    <CloudArrowUpIcon className="mt-1 h-5 w-5 text-indigo-600" aria-hidden="true" />
    <span>
      <strong className="font-semibold text-gray-100">The Main Web App</strong> — Transfer text, photos, and files directly in the browser from any device.
    </span>
  </li>
  <li className="flex gap-3">
    <LockClosedIcon className="mt-1 h-5 w-5 text-indigo-600" aria-hidden="true" />
    <span>
      <strong className="font-semibold text-gray-100">The Mobile Application</strong> — Allows you to share photos and files directly from your mobile device to Cross Copy, skipping the step of opening your mobile browser first.
    </span>
  </li>
  <li className="flex gap-3">
    <ServerIcon className="mt-1 h-5 w-5 text-indigo-600" aria-hidden="true" />
    <span>
      <strong className="font-semibold text-gray-100">The Chrome Extension</strong> — Instantly access copied text, files, and photos right from the Chrome toolbar.
    </span>
  </li>
</ul>